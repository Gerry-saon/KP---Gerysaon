var jmlMotor = 10;
var motorTerjual = 0;

for (var noMotor = 1; noMotor <= jmlMotor; noMotor++) {
    if(noMotor <= 6 ) {
        console.log('No Motor' + noMotor + ' Terjual dengan laris.');
    }
}